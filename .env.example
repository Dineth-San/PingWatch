# Postgres (used by api + worker)
DATABASE_URL=postgres://ping:watch@localhost:5432/pingwatch?sslmode=disable

# API
JWT_SECRET=AUIHpiougouhsadfouhaiYUGAiofpguPAOSHD8yhq9w

# SMTP — used by worker for down/up alerts
# Local dev default: leave blank to use the Mailpit container (http://localhost:8025)
# Gmail (production): set these and remove the mailpit service from docker-compose
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:8080
